import { Link } from "@tanstack/react-router";
import { Camera, ClockClockwise, Gear, House, User } from "lucide-react";
import type { ReactNode } from "react";

import { cn } from "@/lib/utils";

// lucide equivalents (named imports above are aliases defined below)
export {};
